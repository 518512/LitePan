name: Daily Sync Upstream & Build Docker

on:
  # 每天 UTC 2:00 自动运行
  schedule:
    - cron: '0 2 * * *'
  # 手动触发
  workflow_dispatch:

# ⚡ 强制所有 JS Action 使用 Node.js 24，消除弃用 Warning
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  packages: write

jobs:
  # ===== Job 1: 检查上游是否有更新 =====
  check:
    runs-on: ubuntu-latest
    outputs:
      updated: ${{ steps.check.outputs.updated }}

    steps:
      - name: Checkout your fork
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}
          fetch-depth: 0

      - name: Check upstream updates
        id: check
        run: |
          git remote add upstream https://github.com/Ponphil/LitePan.git || true
          git fetch upstream main --tags
          BEHIND=$(git rev-list --count HEAD..upstream/main)
          echo "Upstream is $BEHIND commits ahead"

          if [ "$BEHIND" -eq 0 ]; then
            echo "updated=false" >> $GITHUB_OUTPUT
          else
            echo "updated=true" >> $GITHUB_OUTPUT
          fi

      # ⚡ check Job summary
      - name: Add check summary
        run: |
          if [ "${{ steps.check.outputs.updated }}" = "true" ]; then
            echo "上游源码有更新，需要执行同步和构建。" >> $GITHUB_STEP_SUMMARY
          else
            echo "✅ 上游源码无更新，构建 Job 将被跳过。" >> $GITHUB_STEP_SUMMARY
          fi

  # ===== Job 2: 同步 + 构建 Docker =====
  build:
    needs: check
    if: needs.check.outputs.updated == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout your fork
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_PAT }}
          fetch-depth: 0

      - name: Setup upstream remote
        run: |
          git remote get-url upstream >/dev/null 2>&1 || \
          git remote add upstream https://github.com/Ponphil/LitePan.git
          git fetch upstream main --tags

      - name: Sync with upstream
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git checkout main
          git merge --ff-only upstream/main || echo "没有可 fast-forward 的提交"
          git push origin main || echo "push 可能因为无更新被跳过"

      - name: Generate version tag
        id: version
        run: |
          VERSION=$(jq -r '.version' web/package.json)
          BASE_VERSION=${VERSION%-beta}
          UPSTREAM_SHA=$(git rev-parse --short upstream/main)
          FULL_VERSION="${BASE_VERSION}-${UPSTREAM_SHA}"
          echo "生成版本: $FULL_VERSION"
          echo "version=$FULL_VERSION" >> $GITHUB_OUTPUT

      # ⚡ build Job summary 输出版本信息
      - name: Add build summary
        run: |
          echo "========== Docker 构建信息 ==========" >> $GITHUB_STEP_SUMMARY
          echo "版本号: ${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "带版本标签镜像: ghcr.io/${{ github.repository_owner }}/litepan-unofficial:${{ steps.version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "最新标签镜像: ghcr.io/${{ github.repository_owner }}/litepan-unofficial:latest" >> $GITHUB_STEP_SUMMARY

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/litepan-unofficial
          tags: |
            type=raw,value=latest
            type=raw,value=${{ steps.version.outputs.version }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GH_PAT }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Delete all untagged images
        uses: actions/github-script@v8
        with:
          github-token: ${{ secrets.GH_PAT }}
          script: |
            const owner = context.repo.owner;
            const packageName = 'litepan-unofficial';

            const versions = await github.paginate(
              github.rest.packages.getAllPackageVersionsForPackageOwnedByUser,
              {
                package_type: 'container',
                package_name: packageName,
                username: owner,
                per_page: 100
              }
            );

            for (const version of versions) {
              const tags = version.metadata?.container?.tags || [];
              if (tags.length === 0) {
                console.log(`删除 untagged 镜像版本 ${version.id}`);
                await github.rest.packages.deletePackageVersionForUser({
                  package_type: 'container',
                  package_name: packageName,
                  username: owner,
                  package_version_id: version.id
                });
              }
            }
